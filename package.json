{
  "devDependencies": {
    "jest": "^25.1.0"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "verbose": true,
    "watchPathIgnorePatterns": [
      "<rootDir>/node_modules/"
    ]
  }
}
